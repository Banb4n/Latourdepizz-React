import { StyleSheet } from 'aphrodite';

const zINDEX = StyleSheet.create({
    popup: {
        zIndex: 10,
    },
    normal: {
        zIndex: 1,
    },
    front: {
        zIndex: 2,
    }
});

export default zINDEX;